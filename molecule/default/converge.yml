---
- name: Converge
  remote_user: ansible
  hosts: all
  tasks:
    - name: "Include role zsh"
      import_role:
        name: nekeal.zsh
  vars:
    users:
      - username: zsh-user
        shell: /bin/zsh
        zshrc:
          template: zshrc-default.zsh.j2
        aliases:
          aliases:
            l: ls -lart
